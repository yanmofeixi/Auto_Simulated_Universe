[簡體中文](README.md) | [繁體中文](README_CHT.md) | [English](README_ENG.md)

# Auto Simulated Universe

崩壞：星穹鐵道 - 模擬宇宙 & 差分宇宙 全自動化工具

## ✨ 功能特點

- 🌌 **差分宇宙**：全自動刷取差分宇宙
- 🌠 **模擬宇宙**：全自動刷取普通模擬宇宙
- 🖥️ **圖形界面**：內建 Web GUI 配置面板，無需手動編輯配置檔案
- 🎯 **智能識別**：基於 OCR 的文字識別，支援事件、祝福、奇物等選擇
- ⚡ **GPU 加速**：可選 CUDA 加速 OCR 推理

## ⚠️ 免責聲明

本軟體是一個外部工具，旨在自動化崩壞：星穹鐵道的遊戲玩法。它僅通過現有使用者介面與遊戲互動，不會修改任何遊戲檔案或遊戲程式碼。

本軟體開源、免費，僅供學習交流使用。開發者團隊擁有本專案的最終解釋權。使用本軟體產生的所有問題與本專案與開發者團隊無關。

根據米哈遊的[崩壞：星穹鐵道公平遊戲宣言](https://sr.mihoyo.com/news/111246?nav=news&type=notice)：

> 「嚴禁使用外掛、加速器、腳本或其他破壞遊戲公平性的第三方工具。」
> 「一經發現，米哈遊將視違規嚴重程度及違規次數，採取扣除違規收益、凍結遊戲帳號、永久封禁遊戲帳號等措施。」

## 📋 系統要求

- Windows 10/11（macOS/Linux 僅支援部分功能）
- Python 3.11+
- 螢幕解析度：1920×1080 或更高（視窗化或全螢幕）
- 遊戲設定：關閉 HDR，文字語言選擇簡體中文
- 遊戲介面不能有任何遮擋

## 🚀 快速開始

### 1. 安裝依賴

推薦使用 Anaconda 建立虛擬環境：

```bash
conda create -n asu python=3.13 -y
conda activate asu
pip install -r requirements.txt
```

### 2. 啟動 GUI 配置面板

**Windows 使用者（推薦）**

雙擊 `start_gui.bat` 即可啟動配置面板。

**命令列啟動**

```bash
python gui_server.py
```

瀏覽器會自動開啟 `http://localhost:8520`，在介面中配置各項參數。

### 3. 執行

**使用 GUI 啟動（推薦）**

在配置面板中點擊「啟動差分宇宙」或「啟動模擬宇宙」按鈕。

**使用命令列啟動**

```bash
# 差分宇宙
python run_diver.py

# 模擬宇宙
python run_simul.py
```

**手動配置（進階使用者）**

如需手動配置，複製 `info_example.yml` 為 `info.yml`，然後編輯配置檔案。

## ⚙️ 配置說明

### 通用配置

| 配置項       | 說明                               |
| ------------ | ---------------------------------- |
| `angle`      | 校準值（滑鼠靈敏度倍率），預設 1.0 |
| `difficulty` | 難度 1-5，5 為最高難度             |
| `timezone`   | 時區設定，預設跟隨系統             |

### 差分宇宙配置

| 配置項         | 說明                                         |
| -------------- | -------------------------------------------- |
| `accuracy`     | 圖像識別精度，預設 1440，範圍 960-1920       |
| `ocr_use_gpu`  | OCR 是否使用 GPU 加速                        |
| `team`         | 隊伍類型：追擊/dot/終結技/擊破/盾反/白厄盾丹 |
| `skill`        | 首領房間需要開秘技的角色列表                 |
| `portal_prior` | 傳送門優先順序（1-6，數字越大優先順序越高）  |

### 模擬宇宙配置

| 配置項           | 說明                           |
| ---------------- | ------------------------------ |
| `fate`           | 當前命途                       |
| `secondary_fate` | 次要命途（備選）               |
| `use_consumable` | 是否使用消耗品                 |
| `prior`          | 奇物、事件、祝福的優先順序列表 |

## 🔧 校準

如果出現視角轉動過大/過小而導致迷路的問題，需要進行校準：

1. 進入遊戲，將人物傳送到黑塔的辦公室
2. 執行校準腳本：

```bash
python align_angle.py
```

3. 等待視角轉換/原地轉圈結束

> 注意：改變滑鼠 DPI 可能會影響校準值，需要重新校準。

## 🔔 通知外掛

執行 `notif.py` 可開啟 Windows 通知，每刷完一次都會收到通知：

```bash
python notif.py
```

計數會在每週自動重置。如需手動修改計數，編輯 `logs/notif.txt`。

## 📁 專案結構

```
Auto_Simulated_Universe/
├── start_gui.bat           # GUI 啟動器（雙擊執行）
├── gui_server.py           # GUI 伺服器
├── gui/                    # Web GUI 檔案
│   ├── index.html
│   ├── styles.css
│   └── main.js
├── run_diver.py            # 差分宇宙入口
├── run_simul.py            # 模擬宇宙入口
├── diver/                  # 差分宇宙模組
├── simul/                  # 模擬宇宙模組
├── utils/                  # 通用工具
├── data/                   # 資料檔案
│   ├── defaults.json       # 預設配置
│   ├── characters.json     # 角色列表
│   └── ocr_defaults.json   # OCR 預設詞表
├── info.yml                # 使用者配置檔案
└── info_example.yml        # 配置範例
```
